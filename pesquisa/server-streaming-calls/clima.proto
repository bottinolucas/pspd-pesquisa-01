syntax = "proto3";

service ClimaService {
  rpc MonitorarTemperatura (TemperaturaRequest)
      returns (stream TemperaturaResponse);
}

message TemperaturaRequest {
  string cidade = 1;
}

message TemperaturaResponse {
  int32 temperatura = 1;
}